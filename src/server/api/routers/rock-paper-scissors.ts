import { randomUUID } from "crypto";
import { observable } from "@trpc/server/observable";
import { createTRPCRouter, userProcedure } from "~/server/api/trpc";

export const rockPaperScissors = createTRPCRouter({
    
})
